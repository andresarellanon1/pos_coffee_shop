<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_custom_screen_kanban" model="ir.ui.view">
        <field name="name">pos.config.kanban.view.inherit.coffe_product_screen</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_config_kanban" />
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('o_kanban_card_manage_settings')]" position="before">
                Enter parent session id:
            </xpath>
        </field>
    </record>
</odoo>

